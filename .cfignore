# Cloudflare Pages ignore file
# Excludes files from validation during Cloudflare Pages deployment
# Only files in the 'out' directory (build output) will actually be deployed

# Next.js build artifacts - contains large webpack cache files exceeding 25MB limit
# The .next/cache/webpack/server-production/0.pack file can be 60-70MB
.next/

# Node modules - large directory not needed for deployment
node_modules/
